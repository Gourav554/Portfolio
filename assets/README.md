# Assets folder

Catch-all folder for any misc. static files that don't belong in
`images/`, `icons/`, or `resume/` — e.g. downloadable project files,
brand assets, or a favicon source file. Currently unused; the favicon is
inlined as a data URI in `<head>` of `index.html`.